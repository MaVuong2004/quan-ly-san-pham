<!DOCTYPE html> <html> <head> <meta charset="UTF-8"> <title>Quản lý sản phẩm bằng QR</title> <script src="https://unpkg.com/html5-qrcode"></script> <style> body { font-family: Arial; padding: 20px; } #reader { width: 300px; margin-bottom: 20px; } li { margin: 5px 0; } #manual-input { margin-top: 20px; } </style> </head> <body> <h1>Quét sản phẩm</h1> <div id="reader"></div> <h2>Danh sách sản phẩm:</h2> <ul id="product-list"></ul> <h2>Tổng tiền: <span id="total">0</span> VNĐ</h2> <div id="manual-input"> <input type="text" id="manual-id" placeholder="Nhập mã sản phẩm..."> <button onclick="addManual()">✔ Thêm</button> </div> <script> const sheetURL = "https://docs.google.com/spreadsheets/d/1ZETWL6NP4soFwOMrw03p8o32a3Mbh3tgx--M4sOgZZ0/export?format=csv"; let products = {}; let total = 0; fetch(sheetURL) .then(res => res.text()) .then(csv => { const lines = csv.split("\n"); lines.slice(1).forEach(line => { const [id, name, price] = line.split(","); if (id && name && price) { const cleanId = id.trim().toUpperCase(); const cleanName = name.trim(); const cleanPrice = parseInt(price.trim().replace(/\D/g, "")); if (!isNaN(cleanPrice)) { products[cleanId] = { name: cleanName, price: cleanPrice }; console.log("📦 Đã nạp:", cleanId, products[cleanId]); } else { console.warn("⚠ Không đọc được giá:", line); } } }); }); function addProduct(id) { const key = id.trim().toUpperCase(); if (!products[key]) return alert("❌ Không tìm thấy sản phẩm: " + key); const li = document.createElement("li"); li.textContent = `✔ ${products[key].name} - ${products[key].price} VNĐ`; document.getElementById("product-list").appendChild(li); total += products[key].price; document.getElementById("total").textContent = total; } function addManual() { const input = document.getElementById("manual-id"); const id = input.value; if (!id) return; console.log("🖊 Mã nhập tay:", id); addProduct(id); input.value = ""; } // Kích hoạt camera (chỉ hoạt động khi thiết bị có camera) if (navigator.mediaDevices && navigator.mediaDevices.getUserMedia) { new Html5Qrcode("reader").start( { facingMode: "environment" }, { fps: 10, qrbox: 250 }, decodedText => { const id = decodedText.trim(); console.log("💾 Mã đã quét:", id); addProduct(id); } ).catch(err => console.error("❌ Lỗi camera:", err)); } else { document.getElementById("reader").textContent = "Thiết bị không hỗ trợ camera."; } </script> </body> </html>
