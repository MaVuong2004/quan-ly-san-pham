<!DOCTYPE html> <html lang="vi"> <head> <meta charset="UTF-8"> <title>Quản lý sản phẩm bằng QR</title> <script src="https://unpkg.com/html5-qrcode"></script> <style> body { font-family: Arial, sans-serif; padding: 20px; } #reader { width: 300px; margin-bottom: 20px; } li { margin: 5px 0; } #manual-input { margin-top: 20px; } </style> </head> <body> <h1>Quản lý sản phẩm</h1> <div id="reader"></div> <h2>Danh sách sản phẩm:</h2> <ul id="product-list"></ul> <h2>Tổng tiền: <span id="total">0</span> VNĐ</h2> <div id="manual-input"> <input type="text" id="manual-id" placeholder="Nhập mã sản phẩm..."> <button onclick="addManual()">✔ Thêm</button> </div> <script> const sheetURL = "https://docs.google.com/spreadsheets/d/1ZETWL6NP4soFwOMrw03p8o32a3Mbh3tgx--M4sOgZZ0/export?format=csv"; let products = {}; let total = 0; // Tải dữ liệu từ Google Sheets fetch(sheetURL) .then(res => res.text()) .then(csv => { const lines = csv.split("\n"); lines.slice(1).forEach(line => { const [id, name, price] = line.split(","); if (id && name && price) { const cleanId = id.trim().toUpperCase(); products[cleanId] = { fname: name.trim(), price: parseInt(price.trim()) }; console.log("📥 Đã nạp:", cleanId, products[cleanId]); } }); }) .catch(error => console.error("❌ Lỗi tải dữ liệu:", error)); function addProduct(id) { const key = id.trim().toUpperCase(); if (!products[key]) { alert("❌ Không tìm thấy sản phẩm: " + key); return; } const li = document.createElement("li"); li.textContent = `✔ ${products[key].fname} - ${products[key].price} VNĐ`; document.getElementById("product-list").appendChild(li); total += products[key].price; document.getElementById("total").textContent = total; console.log("✅ Đã thêm sản phẩm:", key, products[key]); } function addManual() { const input = document.getElementById("manual-id"); const id = input.value; if (!id) return; console.log("✍ Nhập tay mã:", id); addProduct(id); input.value = ""; } // Kích hoạt camera quét QR (nếu có) if (navigator.mediaDevices && navigator.mediaDevices.getUserMedia) { new Html5Qrcode("reader").start( { facingMode: "environment" }, { fps: 10, qrbox: 250 }, decodedText => { const id = decodedText.trim(); console.log("📷 Mã đã quét:", id); addProduct(id); } ).catch(err => { console.error("❌ Lỗi khởi động camera:", err); document.getElementById("reader").textContent = "Không thể truy cập camera."; }); } else { document.getElementById("reader").textContent = "Trình duyệt không hỗ trợ camera."; } </script> </body> </html>
